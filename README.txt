README
======

This extension creates two build macros $(SolutionConfiguration) and
&(SolutionPlatform), denoting the active solution configuration name
and platform.

Build is supported only on VS10 and VS15 solutions. VS12 is kept only
for reference. Check also dependencies needed in Resources/Deps/Readme.txt

License is MIT